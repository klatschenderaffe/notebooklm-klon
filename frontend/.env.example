# Kopieren nach .env.local und mit echten Werten füllen. Wird NIE committet (siehe .gitignore).

# URL des Backend-Services (lokal: http://localhost:8000, produktiv: Render-URL)
VITE_API_URL=http://localhost:8000

# Supabase Projekt-Einstellungen -> Project Settings -> Data API
# Der Anon-Key ist bewusst öffentlich/im Frontend sichtbar (anders als der Service-Key
# im Backend) -- das ist sein vorgesehener Einsatzzweck für Supabase Auth im Browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Sentry Error-Monitoring (optional) -> sentry.io -> Project Settings -> Client Keys (DSN)
# Ohne gesetzten DSN bleibt Sentry komplett inaktiv (No-Op), siehe main.tsx.
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=

# Anteil der Anfragen, die für Performance-Tracing vermessen werden (0-1, Standard 1.0).
# In der Staging-/Demo-Phase bewusst hoch; bei echtem Produktivtraffic auf ~0.1-0.2
# reduzieren, um im kostenlosen Sentry-Kontingent (5.000.000 Spans/Monat) zu bleiben.
VITE_SENTRY_TRACES_SAMPLE_RATE=
